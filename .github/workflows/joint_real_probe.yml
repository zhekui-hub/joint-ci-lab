name: joint-real-probe
on:
  workflow_dispatch:
    inputs:
      driver_sha:
        required: true
        type: string
      synapse_sha:
        required: true
        type: string
      sim_sha:
        required: true
        type: string
permissions:
  contents: read
jobs:
  validate-inputs:
    runs-on: ubuntu-latest
    steps:
      - run: echo plan6 fail; exit 1
