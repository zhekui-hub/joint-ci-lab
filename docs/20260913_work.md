## 2026-09-13 REAL GitHub Joint CI feasibility probes

### 修改目标
为 `zhekui-hub` 个人实验室补充可重复的真实 GitHub API 联调入口，覆盖 P1–P5 的可验证步骤，并保持本地 fake E1–E42 门禁不变。

### 修改范围
- `prototype/experiments/real_gh/run_real.py`：gh CLI 白名单、Issue、workflow_dispatch、跨仓 joint-ci Check/status、同 URL 校验和结果报告。
- `.github/workflows/joint_real_probe.yml`：接收 driver/synapse/sim 显式 SHA 的最小探针 workflow。
- `prototype/experiments/real_gh/workflows/participant_joint_probe.yml`：可复制到 mock 仓的 P3 workflow 模板。
- `prototype/experiments/real_gh/RESULTS_REAL.md`：由执行器每次生成，记录 live/dry-run 结果。

### 未改动范围
- 未修改数据库、后端接口、鉴权和计费逻辑。
- 未修改上传/下载、任务队列、现有 `gate.yml` 或本地 fake 实验。
- 未访问或配置 ChipLTech 仓库。

### 验证结果
- `python prototype/experiments/real_gh/run_real.py`：dry-run 通过，未进行网络写入（当前环境无 `JOINT_GH_TOKEN`）。
- `python -m py_compile prototype/experiments/real_gh/run_real.py`：通过。
- `python prototype/experiments/run_local.py --json`：现有 E1–E42 场景全部 PASS。

### 风险与后续检查
- live P1/跨仓 Check 写入需要四仓范围的 PAT 或 GitHub App；默认 `GITHUB_TOKEN` 通常不能写其他私有仓。
- P4 的错峰 PR、等待期 runner 证据仍需操作员按 `CHECKLIST.md` 执行并回填报告。
